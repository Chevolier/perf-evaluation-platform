# Production environment configuration

server:
  debug: false
  port: 5000

logging:
  level: "INFO"
  file: "data/logs/production.log"

cors:
  origins: []  # Set specific production domains

security:
  rate_limiting: true
  max_requests_per_minute: 100